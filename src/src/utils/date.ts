// Month Data
const years: number[] = [...Array(121).keys()].map(i => i + 1950);
const months: number[] = [...Array(12).keys()].map(i => i + 1);
const days: number[] = [...Array(31).keys()].map(i => i + 1);

export {years, months, days}